# Dashboard data (served as static JSON)

Updated by ingest / `scripts/reapply-geocode.mjs` / `npm run gen:seed`.

**After GitHub Pages deploy:** hard-refresh the site (Ctrl/Cmd+Shift+R). Asset hashes change on build; a cached bundle can show stale map pins.
